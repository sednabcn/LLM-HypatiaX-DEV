# HypatiaX JMLR — Python Dependencies
# Pin versions to match the paper's computational environment

# Core symbolic regression
pysr>=0.19.0
sympy>=1.12

# ML / numerics
numpy>=1.26
scikit-learn>=1.4
scipy>=1.12

# LLM integration (optional — needed for LLM-guided mode)
anthropic>=0.20.0

# Validation
pint>=0.23          # dimensional analysis

# Utilities
pandas>=2.1
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0

# Paper / reporting
jupytext>=1.16      # notebook ↔ script conversion
nbconvert>=7.0      # notebook execution
